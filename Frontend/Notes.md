Attributes -> additional properties
border-top / border-right / border-left / border-bottom -> 2px solid black
combinators -> two or more selectors

## p*10>{$}
## ul>li*100{$}

first web browser --> Mosaic-1993 not more success
first web browser --> Netscape-Navigator-1994 more success

JS knows binary languages 0 and 1 . but the compiler/interprator translates them to readable languages so that we can easily understand the things.

whenever a boolean value added to a number, then boolean value will get converted to number. 

